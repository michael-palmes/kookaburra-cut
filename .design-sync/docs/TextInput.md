---
category: Forms
---

Single-line text input: a recessed trough (`--surface-recessed`) whose border strengthens on focus. The standard text field in dialogs and wizards — usually inside a `Field`.

```tsx
<Field label="Project name">
  <TextInput placeholder="launch-2026" />
</Field>
```
