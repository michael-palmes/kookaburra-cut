import { defineScene } from "@kookaburra/toolkit";

/** Animated-background transition fixture: the sidecar's shader background is the content. */
export default defineScene({
  id: "bg-swirl",
  durationMs: 2000,
  Scene() {
    return null;
  },
});
