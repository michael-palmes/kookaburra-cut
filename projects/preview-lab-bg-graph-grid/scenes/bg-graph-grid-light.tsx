import { defineScene } from "@kookaburra/toolkit";

/**
 * Preview Lab — light-mode sample for the "graph-grid" fill (the "Rottnest" p1 preset). DEV-ONLY: rendered by `pnpm kookaburra:run --action option-previews`
 * into the committed picker preview assets (src/assets/option-previews/). UNSTAGED and empty on
 * purpose: the sidecar's shader background IS the content.
 */
export default defineScene({
  id: "lab-bg-graph-grid-light",
  durationMs: 2000,
  Scene() {
    return null;
  },
});
