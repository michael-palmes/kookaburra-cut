import { defineScene } from "@kookaburra/toolkit";

// The claim rides the sidecar's frame band, so the capture fills the cutout unstaged.
export default defineScene({
  id: "app-store-preview-30-02-claim-over-capture",
  durationMs: 4000,
  Scene() {
    return null;
  },
});
