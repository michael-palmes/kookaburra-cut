{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "theme-editor",
  "description": "Capabilities for the Kookaburra Cut theme editor window. Dialog covers the unsaved-changes confirm; every theme file touch goes through path-confined Rust commands, never an fs capability.",
  "windows": ["theme-editor"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-title",
    "dialog:default"
  ]
}
