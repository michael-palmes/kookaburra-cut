import { defineScene, SceneStage, useTheme } from "@kookaburra/toolkit";

/**
 * __NAME__ — scaffolded blank scene. Compose freely (toolkit primitives only — see the
 * kookaburra-scene-authoring skill); machine-editable values belong in the sidecar
 * `scenes/__STEM__.json`. `<SceneStage>` mounts the theme's staging (lights, backdrop,
 * shadows) and honours the sidecar's stage overrides.
 */
export default defineScene({
  id: "__SCENE_ID__",
  durationMs: __DURATION_MS__,
  Scene() {
    const theme = useTheme();
    return (
      <SceneStage>
        <color attach="background" args={[theme.colors.background]} />
      </SceneStage>
    );
  },
});
