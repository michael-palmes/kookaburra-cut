import { defineScene } from "@kookaburra/toolkit";

/**
 * __NAME__: scaffolded video scene. The sidecar `scenes/__STEM__.json` owns the
 * background video (`background: { type: "video" }`), which the scene host mounts on
 * every scene, so the composition stays empty and unstaged. Add toolkit primitives here
 * for overlays (see the kookaburra-scene-authoring skill).
 */
export default defineScene({
  id: "__SCENE_ID__",
  durationMs: __DURATION_MS__,
  Scene() {
    return null;
  },
});
