diff --git a/dist/troika-three-text.esm.js b/dist/troika-three-text.esm.js
index 72c7a8b9e8911a669fd40b9a7d897a74197226ea..a5ccf4fb2625d1f3c3f6c404a824fed13a5d5c39 100644
--- a/dist/troika-three-text.esm.js
+++ b/dist/troika-three-text.esm.js
@@ -494,12 +494,16 @@ function createFontResolver(fontParser, unicodeFontResolverClient) {
    */
   const loadingFonts = Object.create(null);
 
+  // URLs that failed to load or parse; treated as covering no codepoints so resolution can proceed
+  const failedFonts = new Set();
+
   /**
    * Load a given font url
    */
   function doLoadFont(url, callback) {
     const onError = err => {
       console.error(`Failure loading font ${url}`, err);
+      callback(null);
     };
     try {
       const request = new XMLHttpRequest();
@@ -542,8 +546,12 @@ function createFontResolver(fontParser, unicodeFontResolverClient) {
     } else {
       loadingFonts[fontUrl] = [callback];
       doLoadFont(fontUrl, fontObj => {
-        fontObj.src = fontUrl;
-        parsedFonts[fontUrl] = fontObj;
+        if (fontObj) {
+          fontObj.src = fontUrl;
+          parsedFonts[fontUrl] = fontObj;
+        } else {
+          failedFonts.add(fontUrl);
+        }
         loadingFonts[fontUrl].forEach(cb => cb(fontObj));
         delete loadingFonts[fontUrl];
       });
@@ -618,8 +626,9 @@ function createFontResolver(fontParser, unicodeFontResolverClient) {
                 // filter by optional explicit unicode ranges
                 if (!unicodeRange || isCodeInRanges(codePoint, unicodeRange)) {
                   const fontObj = parsedFonts[src];
-                  // font not yet loaded, load it and resume
+                  // font not yet loaded, load it and resume; a failed font covers nothing
                   if (!fontObj) {
+                    if (failedFonts.has(src)) continue;
                     loadFont(src, () => {
                       resolveUserFonts(i);
                     });
@@ -681,12 +690,17 @@ function createFontResolver(fontParser, unicodeFontResolverClient) {
           let loadedCount = 0;
           fontUrls.forEach((url, i) => {
             loadFont(url, fontObj => {
-              fontResolutions[i + fontIndexOffset] = fontObj;
+              // A failed fallback font degrades to the primary font's .notdef
+              fontResolutions[i + fontIndexOffset] = fontObj || fontResolutions[0] || null;
               if (++loadedCount === fontUrls.length) {
                 allDone();
               }
             });
           });
+        }).catch(err => {
+          // Degrade uncovered codepoints to .notdef instead of wedging sync forever when the fallback fetch fails (CSP/offline)
+          console.warn('[troika-three-text] fallback font resolution failed; continuing without fallback fonts', err);
+          allDone();
         });
       } else {
         allDone();
diff --git a/dist/troika-three-text.umd.js b/dist/troika-three-text.umd.js
index 32c3dcbcd9b716fa61c62e783ed89a0d532e23f6..39e4df5de2295772737b90c89293231932709df8 100644
--- a/dist/troika-three-text.umd.js
+++ b/dist/troika-three-text.umd.js
@@ -499,12 +499,16 @@
      */
     const loadingFonts = Object.create(null);
 
+    // URLs that failed to load or parse; treated as covering no codepoints so resolution can proceed
+    const failedFonts = new Set();
+
     /**
      * Load a given font url
      */
     function doLoadFont(url, callback) {
       const onError = err => {
         console.error(`Failure loading font ${url}`, err);
+        callback(null);
       };
       try {
         const request = new XMLHttpRequest();
@@ -547,8 +551,12 @@
       } else {
         loadingFonts[fontUrl] = [callback];
         doLoadFont(fontUrl, fontObj => {
-          fontObj.src = fontUrl;
-          parsedFonts[fontUrl] = fontObj;
+          if (fontObj) {
+            fontObj.src = fontUrl;
+            parsedFonts[fontUrl] = fontObj;
+          } else {
+            failedFonts.add(fontUrl);
+          }
           loadingFonts[fontUrl].forEach(cb => cb(fontObj));
           delete loadingFonts[fontUrl];
         });
@@ -623,8 +631,9 @@
                   // filter by optional explicit unicode ranges
                   if (!unicodeRange || isCodeInRanges(codePoint, unicodeRange)) {
                     const fontObj = parsedFonts[src];
-                    // font not yet loaded, load it and resume
+                    // font not yet loaded, load it and resume; a failed font covers nothing
                     if (!fontObj) {
+                      if (failedFonts.has(src)) continue;
                       loadFont(src, () => {
                         resolveUserFonts(i);
                       });
@@ -686,12 +695,17 @@
             let loadedCount = 0;
             fontUrls.forEach((url, i) => {
               loadFont(url, fontObj => {
-                fontResolutions[i + fontIndexOffset] = fontObj;
+                // A failed fallback font degrades to the primary font's .notdef
+                fontResolutions[i + fontIndexOffset] = fontObj || fontResolutions[0] || null;
                 if (++loadedCount === fontUrls.length) {
                   allDone();
                 }
               });
             });
+          }).catch(err => {
+            // Degrade uncovered codepoints to .notdef instead of wedging sync forever when the fallback fetch fails (CSP/offline)
+            console.warn('[troika-three-text] fallback font resolution failed; continuing without fallback fonts', err);
+            allDone();
           });
         } else {
           allDone();
diff --git a/src/FontResolver.js b/src/FontResolver.js
index 6d5741c8b292a94a77c010d54d58c95823b27658..2b7908cf7c26a9e63ca4a8803d8e1a7e36c76e1e 100644
--- a/src/FontResolver.js
+++ b/src/FontResolver.js
@@ -44,12 +44,16 @@ export function createFontResolver(fontParser, unicodeFontResolverClient) {
    */
   const loadingFonts = Object.create(null)
 
+  // URLs that failed to load or parse; treated as covering no codepoints so resolution can proceed
+  const failedFonts = new Set()
+
   /**
    * Load a given font url
    */
   function doLoadFont(url, callback) {
     const onError = err => {
       console.error(`Failure loading font ${url}`, err)
+      callback(null)
     }
     try {
       const request = new XMLHttpRequest()
@@ -92,8 +96,12 @@ export function createFontResolver(fontParser, unicodeFontResolverClient) {
     } else {
       loadingFonts[fontUrl] = [callback]
       doLoadFont(fontUrl, fontObj => {
-        fontObj.src = fontUrl
-        parsedFonts[fontUrl] = fontObj
+        if (fontObj) {
+          fontObj.src = fontUrl
+          parsedFonts[fontUrl] = fontObj
+        } else {
+          failedFonts.add(fontUrl)
+        }
         loadingFonts[fontUrl].forEach(cb => cb(fontObj))
         delete loadingFonts[fontUrl];
       })
@@ -168,8 +176,9 @@ export function createFontResolver(fontParser, unicodeFontResolverClient) {
                 // filter by optional explicit unicode ranges
                 if (!unicodeRange || isCodeInRanges(codePoint, unicodeRange)) {
                   const fontObj = parsedFonts[src];
-                  // font not yet loaded, load it and resume
+                  // font not yet loaded, load it and resume; a failed font covers nothing
                   if (!fontObj) {
+                    if (failedFonts.has(src)) continue;
                     loadFont(src, () => {
                       resolveUserFonts(i);
                     });
@@ -231,12 +240,17 @@ export function createFontResolver(fontParser, unicodeFontResolverClient) {
           let loadedCount = 0;
           fontUrls.forEach((url, i) => {
             loadFont(url, fontObj => {
-              fontResolutions[i + fontIndexOffset] = fontObj
+              // A failed fallback font degrades to the primary font's .notdef
+              fontResolutions[i + fontIndexOffset] = fontObj || fontResolutions[0] || null
               if (++loadedCount === fontUrls.length) {
                 allDone();
               }
             })
           })
+        }).catch(err => {
+          // Degrade uncovered codepoints to .notdef instead of wedging sync forever when the fallback fetch fails (CSP/offline)
+          console.warn('[troika-three-text] fallback font resolution failed; continuing without fallback fonts', err);
+          allDone();
         });
       } else {
         allDone();
